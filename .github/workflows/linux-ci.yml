name: Linux CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  semantic-only:
    name: Semantic tests
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Swift
        uses: SwiftyLab/setup-swift@v1.14.0
        with:
          swift-version: "6"

      - name: Show Swift version
        run: swift --version

      - name: Generate semantic-only package
        run: swift scripts/ci-semantic-only.swift

      - name: Test Lottie semantics
        run: swift test --package-path .build/ci/semantic-only --no-parallel
