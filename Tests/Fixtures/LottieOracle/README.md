# Lottie Oracle Fixture Corpus

This directory contains the curated source-intent oracle corpus for PureLottie.
Every fixture is intentionally small enough for review and has a committed
`purelottie.lottie-web-intent` snapshot produced by
`Tools/LottieOracle/scripts/extract-intent.mjs` with pinned
`npm:lottie-web@5.13.0`.

The large raw corpus under `Tests/Fixtures/LottieCorpus` is discovery material.
The files in this directory are the vetted regression set: each one isolates a
specific semantic bug class, evidence roles, selected source frames, and the
numeric browser trace used before any PNG comparison.

Evidence role definitions live in
`docs/lottie-format/fixture-evidence-roles.md`.

| Fixture | Status | Roles | Coverage | Purpose | Frames |
| --- | --- | --- | --- | --- | --- |
| `eligible-shape-position` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `animated-position`, `rectangle`, `fill`, `transform` | Proves animated-position, rectangle, fill, transform source-intent coverage and protects: Animated layer position was previously judged from shifted PNGs instead of numeric translation. | 0, 5, 9 |
| `static-rectangle-fill` | modeled | `conformance`, `regression`, `visual-inspection` | `static-position`, `rectangle`, `fill` | Proves static-position, rectangle, fill source-intent coverage and protects: Baseline geometry and fill color must be correct before animation is considered. | 0, 5, 9 |
| `animated-position-linear` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `animated-position`, `rectangle`, `fill`, `transform` | Proves animated-position, rectangle, fill, transform source-intent coverage and protects: Linear position in-betweens must match source-frame interpolation, not image inspection. | 0, 5, 9 |
| `split-position-ellipse` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `split-position`, `ellipse`, `fill`, `transform` | Proves split-position, ellipse, fill, transform source-intent coverage and protects: Split-position values must rejoin into one evaluated layer position before lowering. | 0, 5, 9 |
| `anchor-rotation-rectangle` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `anchor`, `rotation`, `rectangle`, `fill` | Proves anchor, rotation, rectangle, fill source-intent coverage and protects: Anchor translation and clockwise rotation order must agree with lottie-web matrices. | 0, 5, 9 |
| `scale-rotation-anchor` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `anchor`, `scale`, `rotation`, `rectangle` | Proves anchor, scale, rotation, rectangle source-intent coverage and protects: Scale, anchor, and rotation composition must be measurable before target backend assignment. | 0, 5, 9 |
| `animated-opacity-rectangle` | modeled | `conformance`, `regression`, `visual-inspection` | `opacity`, `rectangle`, `fill` | Proves opacity, rectangle, fill source-intent coverage and protects: Opacity must be sampled numerically at the source frame, not inferred from raster alpha by eye. | 0, 5, 9 |
| `group-transform-rectangle` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `shape-transform`, `rectangle`, `fill` | Proves shape-transform, rectangle, fill source-intent coverage and protects: Shape transforms are scoped to the group and must not be mistaken for layer transforms. | 0, 5, 9 |
| `group-opacity-two-shapes` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `shape-group`, `group-opacity`, `rectangle`, `ellipse` | Proves shape-group, group-opacity, rectangle, ellipse source-intent coverage and protects: Group opacity is an atomic compositing fact and must not be flattened silently per shape. | 0, 5, 9 |
| `parent-null-transform-child` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `parent-transform`, `null-layer`, `rectangle`, `fill` | Proves parent-transform, null-layer, rectangle, fill source-intent coverage and protects: Parent transform composition must use the parent layer matrix before the child matrix. | 0, 5, 9 |
| `parent-animated-transform-child` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `parent-transform`, `animated-position`, `null-layer`, `rectangle` | Proves parent-transform, animated-position, null-layer, rectangle source-intent coverage and protects: Animated parent matrices must affect the child world matrix at every sampled frame. | 0, 5, 9 |
| `ellipse-fill` | modeled | `conformance`, `regression`, `visual-inspection` | `ellipse`, `fill` | Proves ellipse, fill source-intent coverage and protects: Ellipse noon-start geometry and bounds must be captured in source space. | 0, 5, 9 |
| `ellipse-reversed-direction` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `ellipse`, `direction`, `fill` | Proves ellipse, direction, fill source-intent coverage and protects: Direction changes trim and path ordering even when the untrimmed bounds look identical. | 0, 5, 9 |
| `rounded-rectangle` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `rectangle`, `roundness`, `fill` | Proves rectangle, roundness, fill source-intent coverage and protects: Rounded rectangle control points require the lottie-web radius clamp and roundCorner constant. | 0, 5, 9 |
| `raw-bezier-triangle` | modeled | `conformance`, `regression`, `visual-inspection` | `path`, `fill` | Proves path, fill source-intent coverage and protects: Raw path vertices and tangents must be preserved without primitive regeneration. | 0, 5, 9 |
| `raw-bezier-cubic` | modeled | `conformance`, `regression`, `visual-inspection` | `path`, `stroke` | Proves path, stroke source-intent coverage and protects: Cubic path length and bounds must come from authored tangents, not a polyline guess. | 0, 5, 9 |
| `polygon-five` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `polygon`, `polystar`, `fill` | Proves polygon, polystar, fill source-intent coverage and protects: Polygon point flooring, rotation, and direction must match lottie-web source geometry. | 0, 5, 9 |
| `star-five` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `star`, `polystar`, `fill` | Proves star, polystar, fill source-intent coverage and protects: Star inner and outer radii must survive source evaluation before any PureDraw lowering. | 0, 5, 9 |
| `fill-rule-evenodd` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `path`, `fill-rule`, `fill` | Proves path, fill-rule, fill source-intent coverage and protects: Fill-rule style facts must be retained because identical vertices can rasterize differently. | 0, 5, 9 |
| `stroke-basic-line` | modeled | `conformance`, `regression`, `visual-inspection` | `path`, `stroke` | Proves path, stroke source-intent coverage and protects: Stroke color, opacity, and width must be measured as style facts separate from fill. | 0, 5, 9 |
| `stroke-caps-joins` | modeled | `conformance`, `regression`, `visual-inspection` | `path`, `stroke`, `line-cap`, `line-join` | Proves path, stroke, line-cap, line-join source-intent coverage and protects: Line caps and joins are render-affecting stroke facts and must not disappear from the trace. | 0, 5, 9 |
| `stroke-dash` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `path`, `stroke`, `dash` | Proves path, stroke, dash source-intent coverage and protects: Dash arrays are numeric stroke facts and must be represented or reported before pixels. | 0, 5, 9 |
| `animated-stroke-width` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `path`, `stroke`, `animated-width` | Proves path, stroke, animated-width source-intent coverage and protects: Animated stroke width must be sampled from source frames before lowerer decisions. | 0, 5, 9 |
| `trim-rectangle-half` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `rectangle`, `stroke`, `trim` | Proves rectangle, stroke, trim source-intent coverage and protects: Trim start/end percentages must map to contour length, not an arbitrary quadrant guess. | 0, 5, 9 |
| `trim-ellipse-quadrant` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `ellipse`, `stroke`, `trim` | Proves ellipse, stroke, trim source-intent coverage and protects: Ellipse trim direction and noon-start ordering must be measurable numerically. | 0, 5, 9 |
| `animated-trim-path` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `path`, `stroke`, `trim`, `animated-trim` | Proves path, stroke, trim, animated-trim source-intent coverage and protects: Animated trim must produce source-frame segment facts before rendered output is trusted. | 0, 5, 9 |
| `layer-window-in-out` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `frame-window`, `rectangle`, `fill` | Proves frame-window, rectangle, fill source-intent coverage and protects: Layer ip/op semantics must be proven at numeric frame boundaries. | 2, 3, 8, 9 |
| `mask-add-rectangle` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `mask`, `rectangle`, `fill` | Proves mask, rectangle, fill source-intent coverage and protects: Mask path, mode, inversion, and opacity are source graph facts before backend masking. | 0, 5, 9 |
| `alpha-matte-rectangle` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `matte`, `rectangle`, `ellipse`, `fill` | Proves matte, rectangle, ellipse, fill source-intent coverage and protects: Track matte source-target relationship must be explicit instead of guessed from layer order. | 0, 5, 9 |
| `precomp-static-child` | modeled | `conformance`, `regression`, `visual-inspection`, `engine-divergence` | `precomp`, `animated-position`, `rectangle`, `fill` | Proves precomp, animated-position, rectangle, fill source-intent coverage and protects: Precomp boundaries and child composition paths must be visible in source intent. | 0, 5, 9 |
| `time-remap-precomp-diagnosed` | diagnosed | `unsupported-feature`, `regression`, `engine-divergence`, `visual-inspection` | `precomp`, `time-remap`, `diagnostic`, `animated-position` | Proves precomp, time-remap, diagnostic, animated-position source-intent coverage and protects: Time remap is a diagnosed semantic boundary until lowering consumes the evaluator exactly. | 0, 5, 9 |
